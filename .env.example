# Local backend URL used during development.
# Override this in Vercel or a deployed environment to your production API URL.
EXPO_PUBLIC_API_URL=http://localhost:8787

# Server-only OpenAI key. Keep this in environment variables, not in the app.
OPENAI_API_KEY=sk-your-key-here
